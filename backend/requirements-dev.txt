-r requirements.txt
pytest>=8.0
httpx>=0.27
# Newer starlette versions moved TestClient onto httpx2 instead of httpx;
# installing both keeps `TestClient` working regardless of which version pip resolves.
httpx2>=2.0
